# Directives (reviewer → implementer)

Reviewer agents write `DIRECTIVES-YYYY-MM-DD.md` here.

- Read-only analysis only when producing these files.
- Implementer (e.g. Grok) applies them to `src/lib/prompts.ts` (and related core) after owner approval.

See `../HANDOFF-REVIEWER.md` for the full handoff prompt and file list.
